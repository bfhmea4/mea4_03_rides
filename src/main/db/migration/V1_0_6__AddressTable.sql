CREATE TABLE ride_request (
                              `id` INTEGER PRIMARY KEY AUTO_INCREMENT,
                              `street` TEXT,
                              `house_number` INTEGER,
                              `postal_code` INTEGER,
                              `location` TEXT
);